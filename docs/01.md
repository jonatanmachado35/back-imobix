# Discovery Arquitetural — Imobix Painel Web

> Preencha as respostas abaixo antes de tomar decisões de implementação.
> Este documento guia as decisões de multi-tenancy, domínio, segurança e governança.

---

## 1. Negócio

**1.1 Qual o domínio principal do produto?**

> Ex: plataforma de gestão para imobiliárias, CRM imobiliário, marketplace de aluguel por temporada...

```
RESPOSTA:
```

**1.2 Quais são os tenants típicos?**

> Ex: imobiliárias pequenas (1–5 usuários), redes regionais, franquias, proprietários independentes...

```
RESPOSTA:
```

---

## 2. Escala

**2.1 Quantos tenants previstos no curto prazo? (6–12 meses)**

```
RESPOSTA:
```

**2.2 Quantos usuários por tenant, em média?**

> Ex: 1 admin + 5 corretores, 1 admin + 20 funcionários...

```
RESPOSTA:
```

---

## 3. Estratégia de Multi-Tenancy

**3.1 Como o tenant será identificado?**

Marque uma opção:

- [ ] Por **token JWT** (tenantId dentro do payload — backend isola por query)
- [ ] Por **subdomínio** (ex: `minhaImobiliaria.imobix.com`)
- [ ] Por **header HTTP** (ex: `X-Tenant-ID`)
- [ ] Combinação (ex: subdomínio + token)

```
RESPOSTA / JUSTIFICATIVA:
```

**3.2 Isolamento lógico ou físico?**

- [ ] **Lógico** — todos os tenants no mesmo banco, separados por `tenantId`
- [ ] **Físico** — banco separado por tenant (maior custo, maior isolamento)

```
RESPOSTA:
```

**3.3 Garantias mínimas esperadas**

Marque o que se aplica:

- [ ] Tenant A nunca vê dados do Tenant B
- [ ] Super Admin vê tudo mas não pode editar dados de tenant sem log de auditoria
- [ ] Limites por plano (ex: max de usuários, max de anúncios)
- [ ] Suspensão de tenant bloqueia login de todos os usuários do tenant

```
OBSERVAÇÕES:
```

---

## 4. Fronteiras de Domínio

**4.1 Contexto "Tenant Management" (Super Admin)**

Quais ações o Super Admin pode fazer?

- [ ] Criar tenant
- [ ] Editar dados do tenant (nome, plano, cores)
- [ ] Suspender tenant (bloqueia login de todos do tenant)
- [ ] Deletar tenant de forma **irrevogável**
- [ ] Reativar tenant suspenso
- [ ] Ver métricas de uso de todos os tenants
- [ ] Impersonar (logar como) um admin de tenant

```
OBSERVAÇÕES:
```

**4.2 Contexto "Branding / Configuração"**

Quem pode alterar as configurações visuais do painel (nome, cores, logo)?

- [ ] Apenas o Admin do tenant
- [ ] Qualquer usuário do tenant
- [ ] Apenas o Super Admin

Onde as configurações ficam salvas?

- [ ] No backend (banco de dados, por tenant)
- [ ] Apenas no localStorage (atual — sem persistência real)

```
RESPOSTA:
```

**4.3 Contexto "Identidade e Onboarding"**

O fluxo de `primeiroAcesso` existe?

- [ ] Sim — Admin novo deve configurar o painel antes de usar
- [ ] Não — Admin começa direto no painel

Quem cria o tenant/admin inicial?

- [ ] Super Admin cria manualmente pelo painel
- [ ] Auto-cadastro público (`/register`)
- [ ] Processo externo (ex: vendas envia link)

```
RESPOSTA:
```

---

## 5. Compliance

**5.1 Há requisitos de LGPD?**

- [ ] Sim — precisamos de consentimento, direito de exclusão, log de acesso a dados pessoais
- [ ] Não por enquanto

```
OBSERVAÇÕES:
```

**5.2 Auditoria**

- [ ] Precisamos de trilha de auditoria para ações do Super Admin (quem fez o quê, quando)
- [ ] Precisamos de log de ações de Admin dentro do tenant
- [ ] Não é necessário por enquanto

```
OBSERVAÇÕES:
```

---

## 6. Governança de Contratos (Frontend ↔ Backend)

**6.1 Versionamento de API**

- [ ] Sem versionamento por enquanto
- [ ] Versão no path (`/v1/...`)
- [ ] Versão por header

```
RESPOSTA:
```

**6.2 Processo de mudança de contrato**

Quando o backend muda um endpoint ou DTO, como o frontend é avisado?

- [ ] Sem processo formal — comunicação direta
- [ ] O Swagger é a fonte de verdade — frontend acompanha
- [ ] Documento de breaking changes obrigatório antes de mudar

```
RESPOSTA:
```

---

## 7. Segurança

**7.1 Autorização por papel — o que cada role pode fazer?**

| Ação | SUPER_ADMIN | ADMIN | USER |
|------|:-----------:|:-----:|:----:|
| Gerenciar tenants | ? | ? | ? |
| Criar funcionários | ? | ? | ? |
| Ver financeiro | ? | ? | ? |
| Criar anúncios | ? | ? | ? |
| Aprovar anúncios | ? | ? | ? |
| Bloquear usuários | ? | ? | ? |
| Alterar branding | ? | ? | ? |

> Preencha com: `sim`, `não`, ou `próprios` (só os próprios registros)

**7.2 Acesso público ao branding do login**

A página de login precisa carregar as cores e logo do tenant **sem autenticação**?

- [ ] Sim — usuário vê a marca da imobiliária antes de logar
- [ ] Não — login é genérico (Imobix padrão)

```
RESPOSTA:
```

---

## 8. Situação Atual vs. Desejada

| Item | Situação Atual | Desejado |
|------|---------------|---------|
| SUPER_ADMIN | Mock hardcoded (`superadmin@imobix.com`) | Usuário real no backend |
| Multi-tenancy | Frontend simula, backend não tem | ? |
| Leads | Frontend tem UI, backend não tem endpoint | ? |
| Anúncios | Frontend chama `/anuncios`, backend tem `/proprietario/properties` | ? |
| Branding por tenant | localStorage only | Persistido no backend |
| Chat | Backend tem `/conversations`, frontend não usa | ? |
| Reservas | Backend tem `/bookings`, frontend não usa | ? |
| Favoritos | Backend tem `/favorites`, frontend não usa | ? |

> Para cada item "?", defina se é prioridade ou descartado.

---

## 9. Próximos Passos (preencher após as respostas)

- [ ] Alinhar decisão de multi-tenancy com o backend
- [ ] Definir roadmap de endpoints prioritários
- [ ] Documentar contrato de autenticação final (com ou sem SUPER_ADMIN real)
- [ ] Decidir quais funcionalidades da API (chat, reservas, favoritos) entram no painel
